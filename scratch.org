#+title: Scratch

* missing ui/ux
** slint
- keyboard shortcuts
- show attribute properties in table (read/write/etc)
- device selection / backend bar
- show "bonds" window
  - show gap name + address + LTK
  - filter adv by bonded
- show advertising data on hover

* feature ideas
- discovery button
- "stop ATT traffic" button / option
  - allows to e.g. do encryption fully before allowing discovery
  - already sort-of implemented for MITM
- uuid name resolution (assigned numbers repo)

* core
- make proper ASDF packages
- dedup code between ltk.lisp and utils.lisp
- make option to run as subprocess
- figure out how to send evts to UI in synchronous host commands
  - eg ui will not get "disconnected" event if we're waiting on cmd rsp

* random code snippets
#+BEGIN_SRC lisp
#+END_SRC

* slint bugs
- resizing a column makes other columns expand wrong way
  - repro'd on slintpad demo
- very very slow textview on windows
  - was fixed a few days ago upstream
- window with dialog opens only auto-sizes on second open/show

* resources
[[https://docs.rs/subprocess/latest/subprocess/][subprocess]]
[[https://doc.rust-lang.org/std/process/struct.Command.html][command]]
[[https://docs.rs/process-wrap/latest/process_wrap/][process_wrap]]
[[https://gist.github.com/Technius/43977937a28e8846d917b53605e32cc3][using tokio]]
